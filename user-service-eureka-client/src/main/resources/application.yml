spring:
  application:
    name: user-service-eureka-client
server:
  port: 0 # random port enable

eureka:
  instance:
    # instance id ? ???? ??? default -> {hostname}:{spring.application.name}:{server.port} ? ???? ??.
    # ??? ??? ?? port ? ?? ??? Server ? ???? Eureka Server ?? ??? ????? ??? ?? ?
    # instance ?? ?? id ? ???? ?? ??? ??? ? ??? ??
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    # Eureka Server ? ?? ?????? ??? ????? ??? ??? ???? ??
    fetch-registry: true
    register-with-eureka: true
    # Eureka Server URL Info
    service-url:
      defaultZone: http://localhost:8761/eureka

